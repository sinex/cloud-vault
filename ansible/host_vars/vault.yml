borg_repo: "{{ lookup('env', 'BORG_REPO') }}"

borg_passphrase: "{{ lookup('env', 'BORG_PASSPHRASE') }}"

borg_ssh_private_key: "{{ lookup('env', 'BORG_SSH_PRIVATE_KEY') }}"

vaultwarden_env: "{{ lookup('file', '../../vaultwarden.env') }}"

cloudflare_zone_id: "{{ lookup('env', 'CLOUDFLARE_ZONE_ID') }}"

cloudflare_token: "{{ lookup('env', 'CLOUDFLARE_API_TOKEN') }}"
